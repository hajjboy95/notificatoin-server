<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body class="container">

<header>
</header>

<main>
    <div class="jumbotron ">
        <h1> <%= title %></h1>
        <% for(var i = 0; i< 10000; i++) {%>
          <h1><%= user %> <%= user %>  <%= user %>   <%= user %></p>
        <% } %>
    </div>
</main>

<footer>
</footer>

</body>
</html>